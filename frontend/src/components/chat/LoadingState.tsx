import { motion } from "framer-motion";
import { Bot } from "lucide-react";
const steps = ["Parsing query...", "Retrieving vulnerabilities...", "Merging sources...", "Ranking results...", "Generating response..."];
export function LoadingState() { return <motion.div initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} className="flex max-w-md gap-3"><div className="grid h-8 w-8 shrink-0 place-items-center rounded-lg bg-blue-500/15 text-blue-300"><Bot className="h-4 w-4" /></div><div className="rounded-2xl rounded-tl-sm border border-border bg-card px-4 py-3"><div className="flex gap-1"><i className="h-1.5 w-1.5 animate-bounce rounded-full bg-blue-400" /><i className="h-1.5 w-1.5 animate-bounce rounded-full bg-blue-400 [animation-delay:150ms]" /><i className="h-1.5 w-1.5 animate-bounce rounded-full bg-blue-400 [animation-delay:300ms]" /></div><p className="mt-2 text-xs text-slate-400">{steps[Math.floor(Date.now() / 1200) % steps.length]}</p></div></motion.div>; }
