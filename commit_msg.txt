feat: containerize API and frontend services

Add a responsive React/Vite frontend for the CyberRAG FastAPI service.

- add a production Dockerfile for the FastAPI backend
- add a multi-stage Dockerfile for the Vite frontend served by Nginx
- proxy frontend chat requests to the API service
- add root Docker Compose configuration for the full stack
- document Docker build, startup, and shutdown workflows
- refresh README retrieval metrics with new reranker results
